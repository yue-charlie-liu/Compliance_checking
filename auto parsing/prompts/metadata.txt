You are given the initial few pages of a regulatory document. You need to extract metadata from it and represent it in a structured JSON format. The JSON should capture only the fields "title", "enactedOn", "jurisdiction", "implementedBy" and "llmSummary". Insert two keys called "numberedSegments" and "content". Both these keys have an empty list as their value. If any of these fields are missing in the input, represent them with an empty string in the output JSON. The "llmSummary" field should contain a descriptive summary of what a reader will learn from the whole document in 4-5 sentences. Note, you are only given initial few pages of document but the summary should be presented as if it represents the entire document. Return only the JSON and no additional text.

Initial few pages of the document as below:
